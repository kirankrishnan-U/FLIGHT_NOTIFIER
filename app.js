document.getElementById('date').value=new Date().toISOString().slice(0,10);async function searchFlight(){flightTitle.textContent=flightInput.value.toUpperCase();const r=await fetch('data/status.json?'+Date.now());const d=await r.json();status.textContent=d.status;origin.textContent=d.origin;scheduled.textContent=d.scheduled;estimated.textContent=d.estimated;terminal.textContent=d.terminal;updated.textContent=new Date().toLocaleTimeString();localStorage.flight=flightInput.value;}flightInput.value=localStorage.flight||'EK532';searchFlight();